## Tooling
- Tests: `npm run test` (Playwright E2E, Chromium). Run `npm run preview -- --port 4321` first, then tests in another terminal.
- Lint/format: `npm run lint:fix` (Biome, single quotes, 2-space indent)
